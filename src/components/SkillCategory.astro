---
import Chip from './Chip.astro';

interface Props {
  category: string;
  items: string[];
  i18nPrefix?: string;
}

const { category, items, i18nPrefix } = Astro.props;
---

<div class="skill-category" data-category={category.toLowerCase()}>
  <h3 class="category-title" data-i18n={i18nPrefix ? `${i18nPrefix}.category` : undefined}>{category}</h3>
  <div class="chips-container">
    {items.map((item, j) => (
      <Chip label={item} i18nKey={i18nPrefix ? `${i18nPrefix}.items.${j}` : undefined} />
    ))}
  </div>
</div>

<style>
  .skill-category {
    padding: var(--spacing-lg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background-color: var(--color-background);
  }

  .category-title {
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: var(--spacing-md);
  }

  .chips-container {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm);
  }
</style>
